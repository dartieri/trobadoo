@model trobadoo.com.web.Models.Common.NavbarModel
@*
@foreach (var menuSection in Model.Sections)
{
    <li id="@menuSection.Id" class="@Html.Raw(menuSection.CssClass)">
        <a href="@menuSection.Url" class="@menuSection.LinkClass">
        @if (menuSection.ShowIcon)
        {
            <span class="menuIcon"><i class="fa @menuSection.IconClass"></i></span>
        }
        @Html.Raw(menuSection.Title)
    </a></li>
}
*@
<div class="navigation-strip">
    <h4>
        Categorías<i class="arrow"></i></h4>
    <div class="top-menu">
        <!-- start header menu -->
        <ul class="megamenu skyblue">
        @foreach (var menuSection in Model.Sections)
        {
            <li id="@menuSection.Id" class="grid"><a class="color1" href="@menuSection.Url">@Html.Raw(menuSection.Title)</a>
                @*<div class="megapanel">
                    <div class="row">
                        <div class="col1">
                            <div class="h_nav">
                                <h4>
                                    shop</h4>
                                <ul>
                                    <li><a href="products.html">new arrivals</a></li>
                                    <li><a href="products.html">men</a></li>
                                    <li><a href="products.html">women</a></li>
                                    <li><a href="products.html">accessories</a></li>
                                    <li><a href="products.html">kids</a></li>
                                    <li><a href="products.html">brands</a></li>
                                </ul>
                            </div>
                        </div>
                    </div>
                </div>*@
            </li>
        }
        </ul>
    </div>
    <div class="clearfix">
    </div>
</div>
